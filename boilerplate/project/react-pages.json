{
  "tools": {
    "npm_bin_dir": "npm bin",
    "browserify_args": "{npm_bin_dir}/babelify {input} -g [ envify --NODE_ENV production ] -g uglifyify -t sassify -t babelify | {npm_bin_dir}/uglifyjs --compress --mangle > {output}",
    "watchify_args": "{npm_bin_dir}/watchify {input} -t sassify -t babelify --outfile {output}"
  },
  "directories": {

  }
}
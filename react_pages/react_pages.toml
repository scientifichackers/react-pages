[commands]
browserify = '{bin}/browserify {input} -t sassify -t babelify'
uglify = '{bin}/uglifyjs --compress --mangle'
watchify =  '{bin}/watchify {input} -t sassify -t babelify -p mapstraction --debug --verbose --outfile {output}'
bin = 'npm bin'